diff --git a/scripts/avalos-update b/scripts/avalos-update
index dc6cf91..0ec480e 100644
--- a/scripts/avalos-update
+++ b/scripts/avalos-update
@@ -24,6 +24,7 @@
 # HYPRLAND_PKGS por avalos-wallpaper), pacman-contrib (checkupdates).
 import json
 import os
+import select
 import subprocess
 import sys
 import threading
@@ -353,7 +354,26 @@ class UpdateAPI:
         except FileNotFoundError:
             self._log("pkexec no está disponible en este sistema.", "err")
             return False
-        for line in proc.stdout:
+        # idle_timeout: el helper ya se protege de colgarse puertas adentro
+        # (ver _stream en avalos-update-helper), pero esto cubre un cuelgue
+        # distinto — pkexec esperando la autenticación (ej. el diálogo de
+        # hyprpolkitagent abrió sin foco en otro workspace) — que solo se
+        # ve desde acá afuera, antes de que el helper llegue a arrancar.
+        idle_timeout = 180
+        while True:
+            listo, _, _ = select.select([proc.stdout], [], [], idle_timeout)
+            if not listo:
+                proc.kill()
+                proc.wait()
+                self._log(
+                    f"sin respuesta por {idle_timeout}s (¿diálogo de "
+                    f"contraseña perdido en otro workspace?) — se canceló.",
+                    "err",
+                )
+                return False
+            line = proc.stdout.readline()
+            if line == "":
+                break
             line = line.rstrip("\n")
             cls = "step" if line.startswith("### ") else "info"
             self._log(line[4:] if line.startswith("### ") else line, cls)
