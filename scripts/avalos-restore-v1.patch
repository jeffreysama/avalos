diff --git a/scripts/avalos-restore b/scripts/avalos-restore
index 12e0f4d..a9236ad 100644
--- a/scripts/avalos-restore
+++ b/scripts/avalos-restore
@@ -19,6 +19,7 @@
 # HYPRLAND_PKGS por avalos-wallpaper), btrfs-progs, snapper (ya
 # instalados por el instalador).
 import json
+import select
 import subprocess
 import sys
 import threading
@@ -253,7 +254,23 @@ class RestoreAPI:
             self._log("pkexec no está disponible en este sistema.", "err")
             return False, []
         captured = []
-        for line in proc.stdout:
+        # idle_timeout: mismo criterio que avalos-update._run_helper — cubre
+        # pkexec colgado esperando auth, no solo lo que pasa adentro del helper.
+        idle_timeout = 180
+        while True:
+            listo, _, _ = select.select([proc.stdout], [], [], idle_timeout)
+            if not listo:
+                proc.kill()
+                proc.wait()
+                self._log(
+                    f"sin respuesta por {idle_timeout}s (¿diálogo de "
+                    f"contraseña perdido en otro workspace?) — se canceló.",
+                    "err",
+                )
+                return False, captured
+            line = proc.stdout.readline()
+            if line == "":
+                break
             line = line.rstrip("\n")
             if line.startswith("### "):
                 rest = line[4:]
